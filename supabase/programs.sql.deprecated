--------------------------------------------------------------------------------
-- DEPRECATED - DO NOT USE
-- This file is kept for reference only
-- The 'programs' table has been replaced by 'programs_lookup' table
-- See: 05_school_management.sql and 10_cleanup_programs_teachers_tables.sql
--------------------------------------------------------------------------------

-- OLD SCHEMA (DEPRECATED):
-- CREATE TABLE IF NOT EXISTS programs (
--   id             bigserial PRIMARY KEY,
--   name           text        NOT NULL,
--   description    text,
--   tracks         integer     DEFAULT 0,
--   courses        integer     DEFAULT 0,
--   avg_class_size integer     DEFAULT 0,
--   status         text        NOT NULL DEFAULT 'Active',
--   created_by     bigint,
--   created_at     timestamptz NOT NULL DEFAULT now()
-- );

-- NEW SCHEMA: Use programs_lookup table instead
-- See 05_school_management.sql for the new programs_lookup table definition
