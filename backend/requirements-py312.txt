# Python 3.12 compatible requirements
# IMPORTANT: Upgrade pip, setuptools, and wheel first:
# python -m pip install --upgrade pip setuptools wheel

# Core API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.0
httpx==0.25.2

# NLP and Text Processing
nltk==3.8.1
spacy>=3.7.3  # 3.7.3+ required for Pydantic 2.x compatibility
textblob==0.17.1
language-tool-python==2.7.1
textstat==0.7.3

# Machine Learning and Data Processing (Python 3.12 compatible versions)
# Note: numpy 1.26+ has pre-built wheels for Python 3.12
numpy>=1.26.0
pandas>=2.1.4
scikit-learn>=1.3.2

# Graph and Network Analysis
networkx==3.2.1

# Additional utilities
pyyaml==6.0.1

# For argument mining and discourse analysis
# Note: spacy-transformers requires C++ build tools on Windows
# If installation fails, you can skip this (argument mining will use basic spaCy features)
# spacy-transformers==1.3.0

# For semantic similarity and embeddings
# Note: sentence-transformers 2.2.2 requires tokenizers which needs Rust
# Using a newer version that has pre-built wheels
sentence-transformers>=2.3.0

# For knowledge graph visualization
pyvis==0.3.2
