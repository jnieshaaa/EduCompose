# EduCompose Backend Requirements
# Python 3.8+ compatible
#
# Installation:
#   pip install -r requirements.txt
#
# For Python 3.12, you may need to upgrade pip first:
#   python -m pip install --upgrade pip setuptools wheel

# ============================================
# Core API Framework
# ============================================
fastapi
uvicorn[standard]
pydantic[email]
python-multipart

# ============================================
# Authentication & Security
# ============================================
python-jose[cryptography]
passlib[bcrypt]

# ============================================
# Database
# ============================================
sqlalchemy
alembic
python-dotenv

# ============================================
# Utilities
# ============================================
python-dotenv
httpx
pyyaml
requests

# ============================================
# NLP and Text Processing Libraries
# ============================================
nltk
spacy
textblob
language-tool-python
textstat

# ============================================
# Machine Learning and Data Processing
# ============================================
scikit-learn
pandas
numpy
scipy

# ============================================
# Graph and Network Analysis
# ============================================
networkx

# ============================================
# Optional / Advanced Features
# ============================================
# For argument mining and discourse analysis
# Note: spacy-transformers requires C++ build tools on Windows
# If installation fails, you can skip this (argument mining will use basic spaCy features)
# spacy-transformers

# For semantic similarity and embeddings (optional, for advanced features)
# Note: sentence-transformers requires tokenizers which needs Rust
# For Python 3.12+, pip will automatically select compatible versions with pre-built wheels
# sentence-transformers

# For transformer-based claim classification
# Note: transformers requires torch (PyTorch) which is a large dependency
# For Python 3.12+, pip will automatically select compatible versions
# transformers
# torch

# For knowledge graph visualization
pyvis

# ============================================
# LLM Support (Optional - for enhanced grammar checking)
# ============================================
# OpenAI API for grammar checking (optional)
# openai

# Google Gemini API for grammar checking (optional)
google-genai

# ============================================
# OCR Support (for PDF and image text extraction)
# ============================================
# PDF text extraction (for text-based PDFs)
PyPDF2
# PDF to image conversion (for scanned PDFs)
pdf2image
# OCR library (Heavy: Consider using Hugging Face API instead)
# easyocr
# Image processing (Pillow is usually already installed)
Pillow

# Poppler installation (required for pdf2image):
#   Windows: winget install oschwartz10612.Poppler
#   Linux: sudo apt-get install poppler-utils
#   macOS: brew install poppler